<!doctype html>
<html lang="en">
  <% include partials/head %>
  <body>
    <% include partials/header %>
    <main>
      <section id="projects">
        <div class="container">
          <header class="section--header">
            <h1 class="text-heading-one">Projects</h1>
          </header>
          <ul class="work-list">
            <% collections.work.forEach(function(work) { %>
            <% if(work.id == 'sna') {} else { %>
            <li class="work card">
              <div class="work-image-wrap">
                <img src="<%= work.pageHeadingImage %>" alt="A screenshot from the <%- work.projectName %> project." class="work-image" />
              </div>
              <h2 class="text-heading-two work-project-tagline"><a href="../work/<%= work.id %>"><%- work.pageHeading %></a></h2>
              <p class="work-client-name"><%- work.clientName %></p>
              <p class="paragraph--minor work-description"><%= work.introduction %></p>
            </li>
            <% } %>
            <% }) %>
        </div>
      </section>
    </main>
    <% include partials/footer %>
  </body>
</html>
